name: Rails Tests

on:
  push:
    # No branch specified - runs on all branches

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.4.3'
        bundler-cache: true

    - name: Install dependencies
      run: bundle install
    
    - name: Set up Chrome
      uses: browser-actions/setup-chrome@latest
    
    - name: Set up Database
      run: |
        rails db:create
        rails db:schema:load
    
    - name: Run tests
      run: rails test:all
